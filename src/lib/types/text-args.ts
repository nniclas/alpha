export interface TextArgs {
    caption?: boolean
    title?: boolean
    xs?: boolean
    sm?: boolean
    md?: boolean
    lg?: boolean
    text?: boolean

    noselect?: boolean

    color?: string

    uc?: boolean // uppercase
    lc?: boolean // lowercase
}
